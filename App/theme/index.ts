import { colors } from './colors'
import { fonts } from './fonts'
import { fontSizes } from './fontSizes'

const theme = {
  colors: {
    ...colors,
  },
  fonts: {
    ...fonts,
  },
  fontSizes: {
    ...fontSizes,
  },
}

export default theme
